# 앱 CSS가 라이트 테마 전용으로 작성되어 있어 테마를 라이트로 고정합니다.
# (고정하지 않으면 OS 다크모드에서 흰 배경 위에 흰 글자가 나옵니다)
[theme]
base            = "light"
primaryColor    = "#F7931A"
backgroundColor = "#F4F6F9"
secondaryBackgroundColor = "#FFFFFF"
textColor       = "#111827"
